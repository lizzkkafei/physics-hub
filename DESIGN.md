# Knowledge Hub — 物理学研究平台 设计文案

## 一、项目概述

**项目名称**：Knowledge Hub（物理研究前沿）

**项目定位**：面向核物理、核天体物理、超核物理等领域研究者的学术内容发布与阅读平台。以 Markdown 为核心内容载体，支持管理员快速上传文章，前端按时间线展示研究动态。

**核心价值**：
- 极简的内容发布流程——上传 `.md` 文件即可发布
- 沉浸式阅读体验——专注学术内容，零干扰排版
- 时间线式浏览——最新研究动态一目了然

---

## 二、目标用户

| 角色 | 需求 | 操作 |
|------|------|------|
| **研究者/读者** | 浏览、阅读物理学研究文章 | 访问首页浏览文章列表 → 点击进入详情阅读 |
| **管理员** | 发布、管理研究文章 | 登录管理后台 → 上传 md 文件 → 发布/删除文章 |

---

## 三、功能规划

### 3.1 前台功能（面向读者）

#### 首页 — 文章时间线
- 按发布时间倒序排列所有文章
- 每篇文章卡片展示：
  - 发布日期
  - 文章标题（可点击，进入详情页）
  - 简介/摘要（取 md 文件前 200 字或 front matter 中的 description 字段）
  - 分类标签（如：核天体物理、超核物理、rp-process 等）
- 顶部固定导航栏：站点名称 + 搜索框 + 管理员入口按钮
- 分页或无限滚动加载

#### 文章详情页
- Markdown 渲染为富文本展示
- 支持 LaTeX 数学公式渲染（KaTeX）
- 代码高亮
- 文章元信息：发布日期、分类、阅读量
- 返回首页按钮
- 上一篇/下一篇文章导航

#### 搜索功能
- 按标题关键词搜索
- 按分类标签筛选

### 3.2 后台功能（面向管理员）

#### 管理员登录
- 简单的用户名/密码登录
- JWT Token 认证
- 登录后跳转管理控制台

#### 管理控制台
- **文章管理**
  - 查看所有文章列表（含发布状态）
  - 删除文章
  - 编辑文章元信息
- **上传文章**
  - 拖拽或点击上传 `.md` 文件
  - 支持批量上传
  - 自动解析 front matter（标题、日期、分类、摘要）
  - 无 front matter 时自动从文件名和内容推断
  - 上传预览：渲染 md 内容，确认后发布
- **系统设置**
  - 修改管理员密码
  - 站点基本信息配置

---

## 四、页面结构

```
Knowledge Hub/
├── 首页 (/)
│   ├── 顶部导航栏
│   ├── 英雄区域 (Hero) — 站点标语 + 背景
│   ├── 文章时间线列表
│   └── 页脚
├── 文章详情页 (/article/:slug)
│   ├── 顶部导航栏
│   ├── 文章元信息区
│   ├── Markdown 渲染正文
│   └── 文章导航
├── 管理员登录页 (/admin/login)
│   └── 登录表单
└── 管理控制台 (/admin/dashboard)
    ├── 侧边栏导航
    ├── 文章列表管理
    ├── 上传文章面板
    └── 系统设置面板
```

---

## 五、Markdown 文件规范

文章 `.md` 文件支持 YAML front matter：

```markdown
---
title: "rp-process等候点核的质量测量进展"
date: 2026-05-09
category: 核天体物理
tags: [rp-process, 等候点, 质量测量]
description: "本文综述了rp-process中关键等候点核⁶⁴Ge、⁶⁸Se、⁷²Kr的质量测量最新进展..."
---

## 正文内容

这里是文章的正文，支持标准 Markdown 语法...

$$E = mc^2$$
```

**自动推断规则**（无 front matter 时）：
- `title`：取文件名（去除 `.md` 后缀和日期前缀）
- `date`：取文件名中的日期前缀（如 `2026-05-09-标题.md`）或文件创建时间
- `category`：默认"未分类"
- `description`：取正文前 200 字符

---

## 六、技术选型

| 层面 | 技术方案 | 说明 |
|------|----------|------|
| **前端** | HTML5 + CSS3 + Vanilla JS | 轻量、无框架依赖、加载快 |
| **CSS框架** | 自定义设计系统 | 深色主题，科研风格 |
| **Markdown渲染** | marked.js | 前端 md → HTML |
| **数学公式** | KaTeX | LaTeX 公式渲染 |
| **代码高亮** | Prism.js | 学术论文中的代码块 |
| **后端** | Python + Flask | 轻量 Web 框架 |
| **数据存储** | 文件系统 + JSON 索引 | 无需数据库，md 文件即数据 |
| **认证** | JWT (PyJWT) | 管理员 Token 认证 |
| **部署** | 本地开发 / 可容器化 | Flask 内置服务器即可运行 |

---

## 七、UI 设计规范

### 7.1 色彩体系

```
主色调（Primary）：    #00D4AA — 科技感青绿色
辅助色（Secondary）：  #6366F1 — 紫色强调
背景色（Background）： #0F1117 — 深邃黑蓝
卡片背景（Card）：     #1A1D2E — 暗色卡片
文字主色（Text）：     #E2E8F0 — 柔和白
文字辅色（Text-Muted）：#94A3B8 — 灰蓝
边框色（Border）：     #2D3348 — 暗色边框
成功色（Success）：    #10B981 — 绿色
警告色（Warning）：    #F59E0B — 黄色
错误色（Error）：      #EF4444 — 红色
```

### 7.2 字体

- 标题：`"Inter", "Noto Sans SC", sans-serif`
- 正文：`"Inter", "Noto Sans SC", serif`
- 代码：`"JetBrains Mono", "Fira Code", monospace`

### 7.3 间距与布局

- 页面最大宽度：`1200px`
- 内容区域：`800px`（文章详情）
- 卡片间距：`24px`
- 卡片内边距：`24px`
- 圆角：`12px`（卡片）、`8px`（按钮）

### 7.4 视觉风格

- 深色主题为主，营造专业学术氛围
- 卡片式布局，微妙的毛玻璃效果
- 细腻的渐变背景和微妙的光晕效果
- 文章列表采用时间线视觉隐喻
- 鼠标悬停时卡片有轻微上浮动画

---

## 八、交互流程

### 8.1 读者浏览流程

```
访问首页 → 浏览文章时间线
    → 点击文章标题 → 进入文章详情页
        → 阅读 Markdown 渲染内容
        → 点击"返回"回到首页
    → 使用搜索框 → 输入关键词 → 过滤文章列表
    → 点击分类标签 → 筛选该分类文章
```

### 8.2 管理员发布流程

```
点击"管理员入口" → 进入登录页
    → 输入用户名/密码 → 认证成功
        → 进入管理控制台
            → 点击"上传文章"
                → 选择/拖拽 .md 文件
                → 系统自动解析 front matter
                → 预览渲染效果
                → 确认发布
            → 管理已有文章（删除/编辑元信息）
```

---

## 九、数据架构

### 9.1 文章索引（articles_index.json）

```json
{
  "articles": [
    {
      "id": "20260509-rp-process-waiting-point",
      "slug": "rp-process-waiting-point",
      "title": "rp-process等候点核的质量测量进展",
      "date": "2026-05-09",
      "category": "核天体物理",
      "tags": ["rp-process", "等候点", "质量测量"],
      "description": "本文综述了rp-process中关键等候点核⁶⁴Ge、⁶⁸Se、⁷²Kr的质量测量最新进展...",
      "filename": "2026-05-09-rp-process-waiting-point.md",
      "published": true
    }
  ]
}
```

### 9.2 管理员配置（config.json）

```json
{
  "admin": {
    "username": "admin",
    "password_hash": "bcrypt_hash..."
  },
  "site": {
    "name": "Knowledge Hub",
    "subtitle": "物理研究前沿",
    "description": "核物理·核天体物理·超核物理 研究动态"
  }
}
```

---

## 十、API 设计

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/api/articles` | 获取文章列表 | 否 |
| GET | `/api/articles/:slug` | 获取文章详情（含渲染后HTML） | 否 |
| GET | `/api/articles/:slug/raw` | 获取文章原始 md 内容 | 是 |
| POST | `/api/admin/login` | 管理员登录 | 否 |
| POST | `/api/admin/upload` | 上传 md 文章 | 是 |
| DELETE | `/api/admin/articles/:slug` | 删除文章 | 是 |
| PUT | `/api/admin/articles/:slug` | 更新文章元信息 | 是 |
| GET | `/api/search?q=keyword` | 搜索文章 | 否 |
| GET | `/api/categories` | 获取分类列表 | 否 |

---

## 十一、开发里程碑

| 阶段 | 内容 | 交付物 |
|------|------|--------|
| **Phase 1** | 设计文案 + 前端页面 | HTML/CSS/JS 静态页面 |
| **Phase 2** | 后端服务 | Flask API 服务 |
| **Phase 3** | 前后端联调 | 完整可用应用 |
| **Phase 4** | 示例文章 + 测试 | 带示例数据的可演示版本 |

---

## 十二、安全考量

- 管理员密码使用 bcrypt 哈希存储
- JWT Token 设置合理过期时间（24h）
- 文件上传限制仅接受 `.md` 格式
- 上传文件大小限制（5MB）
- 管理接口需验证 Token
- XSS 防护：Markdown 渲染时过滤危险 HTML 标签
